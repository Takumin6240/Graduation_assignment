# Scratch学習支援システム - 採点システム解説 v2.1

**対象読者**: 教授、学生、プログラミング未経験者
**作成日**: 2025年10月31日
**バージョン**: 2.1（変数マッピング対応版）

---

## 📚 目次

1. [v2.1の主な改善点](#1-v21の主な改善点)
2. [変数名を無視した採点の仕組み](#2-変数名を無視した採点の仕組み)
3. [採点の詳細フロー](#3-採点の詳細フロー)
4. [実際の採点例（v2.1）](#4-実際の採点例v21)
5. [点数の計算方法](#5-点数の計算方法)
6. [技術的な実装](#6-技術的な実装)

---

## 1. v2.1の主な改善点

### 🎯 最大の改善: 変数名を完全に無視

**v2.0まで（旧バージョン）**:
```
正解: 変数「カウンター」を0にする
生徒: 変数「かうんたー」を0にする

→ 正規化（カタカナ化）して「カウンター」に統一
→ 正解 ✓
```

**問題点**: 変数名が全く違う場合は不正解になる
```
正解: 変数「カウンター」を0にする
生徒: 変数「りんご」を0にする

→ 「カウンター」と「りんご」は一致しない
→ 不正解 ✗（本当は正解にすべき）
```

---

**v2.1（新バージョン）**:
```
正解: 変数「カウンター」を0にする → 1ずつ変える
生徒: 変数「りんご」を0にする → 1ずつ変える

→ 変数の使い方が同じ
→ 正解 ✓
```

### 🔑 重要な考え方

Scratchでは**変数名は何でもいい**。重要なのは「変数をどう使っているか」です。

```
変数名: どうでもいい ❌
変数の役割: 重要 ✓

例:
- 変数を0にする（初期化）
- 変数を1ずつ変える（カウントアップ）
- 変数を表示する

この3つの操作が正しければ、変数名が「カウンター」でも「りんご」でも
「A」でも「x」でも正解！
```

---

## 2. 変数名を無視した採点の仕組み

### 2.1 変数マッピングとは

**変数マッピング** = 正解プログラムの変数と、生徒プログラムの変数を対応付けること

```
【正解プログラム】
変数「カウンター」:
  1. カウンターを0にする (data_setvariableto, 値: 0)
  2. カウンターを1ずつ変える (data_changevariableby, 値: 1)
  3. カウンターを表示する (data_showvariable)

【生徒のプログラム】
変数「りんご」:
  1. りんごを0にする (data_setvariableto, 値: 0)
  2. りんごを1ずつ変える (data_changevariableby, 値: 1)
  3. りんごを表示する (data_showvariable)

→ 使用パターンが一致！
→ 「カウンター」と「りんご」をマッピング
   （内部的に同じ変数として扱う）
```

### 2.2 マッピングの判定基準

2つの変数がマッピングされる条件：

| 条件 | 説明 | 例 |
|-----|------|-----|
| **opcode（操作）が一致** | 同じブロックを使っている | 両方とも「○を○にする」ブロック |
| **値が一致** | 設定する値が同じ | 両方とも「0」にする |
| **使用回数が一致** | 同じ回数使われている | 両方とも3回使用 |
| **使用順序が一致** | 同じ順番で使われている | 初期化 → 変更 → 表示 |

**類似度の計算**:
```
類似度 = 一致する操作の数 ÷ 総操作数

例:
正解: 3つの操作（初期化、変更、表示）
生徒: 3つの操作（初期化、変更、表示） → 全て一致

類似度 = 3 / 3 = 1.0（100%一致）

→ 50%以上（0.5以上）であればマッピング
```

### 2.3 複数の変数がある場合

```
【正解プログラム】
変数A「スコア」: 0にする → 10ずつ変える → 表示
変数B「回数」: 0にする → 1ずつ変える

【生徒のプログラム】
変数X「てんすう」: 0にする → 10ずつ変える → 表示
変数Y「かいすう」: 0にする → 1ずつ変える

→ マッピング結果:
  「スコア」 ↔ 「てんすう」
  「回数」 ↔ 「かいすう」
```

システムが自動的に、最も類似した変数同士を対応付けます。

---

## 3. 採点の詳細フロー（v2.1）

```
┌─────────────────────────────────────────────┐
│  生徒がScratchプログラムを作成               │
│  (.sb3ファイルとして保存)                    │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  システムにアップロード                      │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 1: ファイルを解析                     │
│  (.sb3ファイルはZIPファイル)                 │
│  → project.jsonを取り出す                   │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 2: 変数使用パターンを抽出 ★NEW★      │
│  【正解プログラム】                         │
│    変数「カウンター」:                      │
│      - data_setvariableto (値: 0)          │
│      - data_changevariableby (値: 1)       │
│      - data_showvariable                   │
│                                            │
│  【生徒プログラム】                         │
│    変数「りんご」:                          │
│      - data_setvariableto (値: 0)          │
│      - data_changevariableby (値: 1)       │
│      - data_showvariable                   │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 3: 変数をマッピング ★NEW★            │
│  類似度を計算して、最も似ている変数同士を   │
│  対応付ける                                 │
│                                            │
│  マッピング結果:                            │
│    「カウンター」(正解) ↔ 「りんご」(生徒) │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 4: プログラムを正規化                 │
│  変数名を内部IDに置き換える                 │
│                                            │
│  【正解プログラム】                         │
│    変数__VAR_001__を0にする                 │
│    変数__VAR_001__を1ずつ変える             │
│    変数__VAR_001__を表示する                │
│                                            │
│  【生徒プログラム】（マッピング適用後）     │
│    変数__VAR_001__を0にする                 │
│    変数__VAR_001__を1ずつ変える             │
│    変数__VAR_001__を表示する                │
│                                            │
│  → 変数名が統一された！                    │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 5: 正解データから要件を抽出           │
│  「このブロックが必要」                     │
│  「この順序である必要がある」               │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 6: 提出されたプログラムをチェック      │
│  ① 必要なブロックがあるか？                 │
│  ② ブロックのパラメータは正しいか？         │
│  ③ ブロックの順序は正しいか？               │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 7: 点数を計算                         │
│  各チェック項目ごとに配点                   │
│  合計100点満点                              │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  STEP 8: フィードバックを生成               │
│  ✓ 正しい部分                              │
│  △ 部分的に正しい部分                      │
│  ✗ 間違っている部分                        │
│  💡 修正のヒント                            │
└─────────────┬───────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  結果を生徒に表示                           │
│  - 点数 (0-100点)                          │
│  - 正解/不正解                              │
│  - 詳細なフィードバック                     │
│  - 獲得した経験値                           │
└─────────────────────────────────────────────┘
```

---

## 4. 実際の採点例（v2.1）

### 例1: 変数名が完全に違うが正解

**問題**: 変数を0にして、10回繰り返して1ずつ増やし、最後に表示する

**正解プログラム**:
```
緑の旗がクリックされたとき
  変数「カウンター」を0にする
  10回繰り返す
    変数「カウンター」を1ずつ変える
  変数「カウンター」を表示する
```

**生徒の提出**:
```
緑の旗がクリックされたとき
  変数「りんご」を0にする  ← 名前が違う！
  10回繰り返す
    変数「りんご」を1ずつ変える  ← 名前が違う！
  変数「りんご」を表示する  ← 名前が違う！
```

**v2.1の採点結果**:

```
【Step 1: 変数使用パターンを抽出】
正解: 変数「カウンター」
  - data_setvariableto (値: 0)
  - data_changevariableby (値: 1)
  - data_showvariable

生徒: 変数「りんご」
  - data_setvariableto (値: 0)
  - data_changevariableby (値: 1)
  - data_showvariable

【Step 2: 変数をマッピング】
類似度: 3 / 3 = 1.0（100%一致）
→ 「カウンター」と「りんご」をマッピング

【Step 3: 正規化後】
両方とも「変数__VAR_001__」として統一
→ 完全一致！

【採点結果】
✓ 「緑の旗がクリックされたとき」ブロックがあります
✓ 「○を○にする」ブロックがあります（変数の初期化）
✓ 「10回繰り返す」ブロックがあります
✓ 「○を○ずつ変える」ブロックがあります（変数の変更）
✓ 「○を表示する」ブロックがあります
✓ ブロックの順序が正しいです

点数: 100点
判定: 正解！

💬 変数名は「りんご」でしたが、使い方が正しいので正解です！
```

---

### 例2: 複数の変数がある場合

**問題**: スコアと回数を管理するプログラム

**正解プログラム**:
```
緑の旗がクリックされたとき
  変数「スコア」を0にする
  変数「回数」を0にする
  10回繰り返す
    変数「スコア」を10ずつ変える
    変数「回数」を1ずつ変える
  変数「スコア」を表示する
  変数「回数」を表示する
```

**生徒の提出**:
```
緑の旗がクリックされたとき
  変数「A」を0にする  ← 名前が違う！
  変数「B」を0にする  ← 名前が違う！
  10回繰り返す
    変数「A」を10ずつ変える
    変数「B」を1ずつ変える
  変数「A」を表示する
  変数「B」を表示する
```

**v2.1の採点結果**:

```
【Step 1: 変数使用パターンを抽出】
正解:
  変数「スコア」:
    - data_setvariableto (値: 0)
    - data_changevariableby (値: 10) ← 増加量が10
    - data_showvariable

  変数「回数」:
    - data_setvariableto (値: 0)
    - data_changevariableby (値: 1) ← 増加量が1
    - data_showvariable

生徒:
  変数「A」:
    - data_setvariableto (値: 0)
    - data_changevariableby (値: 10) ← 増加量が10
    - data_showvariable

  変数「B」:
    - data_setvariableto (値: 0)
    - data_changevariableby (値: 1) ← 増加量が1
    - data_showvariable

【Step 2: 変数をマッピング】
「スコア」と「A」の類似度: 1.0（増加量が10で一致）
「回数」と「B」の類似度: 1.0（増加量が1で一致）

マッピング結果:
  「スコア」 ↔ 「A」
  「回数」 ↔ 「B」

【Step 3: 正規化後】
正解:
  変数__VAR_001__を0にする（スコア）
  変数__VAR_002__を0にする（回数）
  ...

生徒（マッピング適用後）:
  変数__VAR_001__を0にする（A → スコア）
  変数__VAR_002__を0にする（B → 回数）
  ...

→ 完全一致！

【採点結果】
点数: 100点
判定: 正解！

💬 変数名は「A」「B」でしたが、それぞれの役割が正しいので正解です！
```

---

### 例3: 変数の使い方が違う場合（不正解の例）

**問題**: 変数を0にして、10回繰り返して1ずつ増やす

**正解プログラム**:
```
緑の旗がクリックされたとき
  変数「カウンター」を0にする
  10回繰り返す
    変数「カウンター」を1ずつ変える
```

**生徒の提出**:
```
緑の旗がクリックされたとき
  変数「りんご」を0にする
  10回繰り返す
    変数「りんご」を5にする  ← 「1ずつ変える」ではなく「5にする」
```

**v2.1の採点結果**:

```
【Step 1: 変数使用パターンを抽出】
正解: 変数「カウンター」
  - data_setvariableto (値: 0)
  - data_changevariableby (値: 1) ← 「変える」ブロック

生徒: 変数「りんご」
  - data_setvariableto (値: 0)
  - data_setvariableto (値: 5) ← 「にする」ブロック（違う操作！）

【Step 2: 変数をマッピング】
類似度: 1 / 2 = 0.5（50%一致）
→ ギリギリマッピング可能

【Step 3: 正規化後】
正解:
  変数__VAR_001__を1ずつ変える

生徒:
  変数__VAR_001__を5にする

→ 操作が違う！

【採点結果】
✓ 「緑の旗がクリックされたとき」ブロックがあります
✓ 「○を○にする」ブロックがあります（初期化）
✓ 「10回繰り返す」ブロックがあります
✗ 「○を○ずつ変える」ブロックがありません
△ 代わりに「○を○にする」ブロックが使われています

点数: 60点
判定: 不正解

💡 ヒント:
「データ」カテゴリから「○を○ずつ変える」ブロックを追加してみましょう
繰り返しの中で使うブロックを確認してください
```

---

## 5. 点数の計算方法

### 5.1 配点の内訳（変更なし）

```
総合点数 = (ブロック要件の得点 + 順序制約の得点) ÷ 満点 × 100
```

#### ブロック要件の配点

- **完全一致**: 10点
- **部分一致**: 5点
- **不一致**: 0点

#### 順序制約の配点

- **順序が正しい**: 5点
- **順序が間違っている**: 0点

### 5.2 変数マッピングの影響

**v2.0（旧）**:
```
正解: 変数「カウンター」を0にする
生徒: 変数「りんご」を0にする

→ 変数名が一致しない → 0点
```

**v2.1（新）**:
```
正解: 変数「カウンター」を0にする
生徒: 変数「りんご」を0にする

→ マッピング適用
→ 両方とも「変数__VAR_001__を0にする」
→ 完全一致 → 10点
```

**結果**: 変数名が違うだけで減点されることがなくなりました！

---

## 6. 技術的な実装

### 6.1 変数マッピングのアルゴリズム

```javascript
function mapVariables(correctUsage, submittedUsage) {
  // 各提出変数に対して、最も類似した正解変数を見つける

  for (提出変数 of 提出変数リスト) {
    let bestMatch = null;
    let bestScore = -1;

    for (正解変数 of 正解変数リスト) {
      // 類似度を計算
      const score = calculatePatternSimilarity(
        提出変数の使用パターン,
        正解変数の使用パターン
      );

      if (score > bestScore) {
        bestScore = score;
        bestMatch = 正解変数;
      }
    }

    // 類似度が50%以上ならマッピング
    if (bestScore >= 0.5) {
      mapping[提出変数ID] = 正解変数ID;
    }
  }

  return mapping;
}
```

### 6.2 類似度の計算

```javascript
function calculatePatternSimilarity(pattern1, pattern2) {
  let matchCount = 0;
  const maxLength = Math.max(pattern1.length, pattern2.length);

  for (let i = 0; i < Math.min(pattern1.length, pattern2.length); i++) {
    const usage1 = pattern1[i];
    const usage2 = pattern2[i];

    // opcodeが一致
    if (usage1.opcode === usage2.opcode) {
      matchCount += 0.5;

      // 値も一致
      if (usage1.value === usage2.value) {
        matchCount += 0.5;
      }
    }
  }

  return matchCount / maxLength;
}
```

### 6.3 変数名の置き換え

```javascript
// マッピング適用前
fields: {
  VARIABLE: "りんご"  // 生徒が付けた変数名
}

// マッピング適用後
fields: {
  VARIABLE: "__VAR_001__"  // 内部ID（正解と統一）
}
```

---

## まとめ

### v2.1の改善効果

| 項目 | v2.0（旧） | v2.1（新） | 改善 |
|-----|----------|----------|------|
| 変数名が違う場合 | 不正解になる可能性 | 正解になる | ✅ |
| 複数変数の対応 | 名前が一致しないと困難 | 自動でマッピング | ✅ |
| 小学生の自由度 | 変数名を覚える必要 | 好きな名前でOK | ✅ |
| 採点の柔軟性 | 低い | 高い | ✅ |
| 教育効果 | 形式的 | 本質的 | ✅ |

### 重要な考え方

**プログラミングで大切なこと**:
1. 変数名は何でもいい（わかりやすい名前が望ましいが、動作には影響しない）
2. 重要なのは「どう使うか」
3. 小学生が自由に名前を付けて、創造性を発揮できる

**v2.1の採点システムは、この考え方を実現しています！**

---

**ドキュメント作成者**: Claude Code
**最終更新日**: 2025年10月31日
**バージョン**: 2.1

このドキュメントに関する質問や改善提案は、プロジェクトのGitHubリポジトリまでお願いします。
